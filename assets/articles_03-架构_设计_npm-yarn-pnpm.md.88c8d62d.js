import{_ as e,c as d,o,a as l}from"./app.948525b9.js";const k=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[{"level":2,"title":"npm install 执行流程","slug":"npm-install-执行流程","link":"#npm-install-执行流程","children":[]},{"level":2,"title":"所有依赖安装到depen....不区分dev会有什么问题？","slug":"所有依赖安装到depen-不区分dev会有什么问题","link":"#所有依赖安装到depen-不区分dev会有什么问题","children":[]},{"level":2,"title":"同时用npm和yarn会有什么问题？","slug":"同时用npm和yarn会有什么问题","link":"#同时用npm和yarn会有什么问题","children":[]},{"level":2,"title":"是否应该提交lockfiles到仓库？ 删除node_modules和lockfiles，重新install，是否有风险？","slug":"是否应该提交lockfiles到仓库-删除node-modules和lockfiles-重新install-是否有风险","link":"#是否应该提交lockfiles到仓库-删除node-modules和lockfiles-重新install-是否有风险","children":[]},{"level":2,"title":"npm/yarn 扁平化方案","slug":"npm-yarn-扁平化方案","link":"#npm-yarn-扁平化方案","children":[{"level":3,"title":"解决了原问题","slug":"解决了原问题","link":"#解决了原问题","children":[]},{"level":3,"title":"带来了新问题","slug":"带来了新问题","link":"#带来了新问题","children":[]}]},{"level":2,"title":"pnpm","slug":"pnpm","link":"#pnpm","children":[{"level":3,"title":"概念 inode","slug":"概念-inode","link":"#概念-inode","children":[]},{"level":3,"title":"概念 hard/soft link","slug":"概念-hard-soft-link","link":"#概念-hard-soft-link","children":[]},{"level":3,"title":"pnpm install 安装过程","slug":"pnpm-install-安装过程","link":"#pnpm-install-安装过程","children":[]},{"level":3,"title":"解决 幽灵依赖 问题","slug":"解决-幽灵依赖-问题","link":"#解决-幽灵依赖-问题","children":[]},{"level":3,"title":"解决分身依赖问题","slug":"解决分身依赖问题","link":"#解决分身依赖问题","children":[]},{"level":3,"title":"更快的原因","slug":"更快的原因","link":"#更快的原因","children":[]},{"level":3,"title":"包存储在了 store 中，为什么我的  node_modules  还是占用了磁盘空间？","slug":"包存储在了-store-中-为什么我的-node-modules-还是占用了磁盘空间","link":"#包存储在了-store-中-为什么我的-node-modules-还是占用了磁盘空间","children":[]}]},{"level":2,"title":"参考资料","slug":"参考资料","link":"#参考资料","children":[]}],"relativePath":"articles/03-架构/设计/npm-yarn-pnpm.md"}'),c={name:"articles/03-架构/设计/npm-yarn-pnpm.md"},n=l(`<h2 id="npm-install-执行流程" tabindex="-1">npm install 执行流程 <a class="header-anchor" href="#npm-install-执行流程" aria-hidden="true">#</a></h2><p><img src="https://kingan-md-img.oss-cn-guangzhou.aliyuncs.com/blog/20230112160326.png" alt=""></p><ol><li>检查 npm 的环境配置(.npmrc) <ul><li>一般都是默认的安装npm时的电脑配置</li><li>手动设置一般只会设置 <code>npm</code> 源，如设置成淘宝镜像</li><li>配置优先级: <code>项目级的.npmrc文件 &gt; 用户级的 .npmrc文件 &gt; 全局级的 .npmrc &gt; npm内置的 .npmrc 文件</code></li></ul></li><li>检查 <code>lockfiles</code><ul><li>并不是检查 <code>lockfiles</code> 是否存在, ❌ 有 <code>lockfiles</code> 就按照该文件安装依赖</li><li>而是检查 <code>lockfiles</code> 中的依赖清单, 是否和 <code>package.json</code> 中的依赖清单一致</li><li>一致, 则按照 <code>lockfiles</code> 的依赖清单安装依赖</li><li>不一致, 不同版本的 npm 做的事情不一样 <ul><li>npm v5.0x 不一致就直接以 <code>package.json</code> 依赖清单为准</li><li>npm v5.1.0 - v5.4.2 不一致 TODO: 看不懂</li><li>npm v5.4.2 以上 判断 <code>package.json</code> 依赖清单定义的版本允许 <code>lockfiles</code> 中的依赖版本, 则按 <code>lockfiles</code> 安装, 如果不允许则按 <code>package.json</code> 安装</li></ul></li></ul></li><li>遍历获取远程依赖包信息, 构建该依赖包的依赖树信息(TODO: 下载前还有一个仅获取依赖信息的网络请求?)，把依赖分析后的依赖树扁平化有利于去重(依赖名和版本都要相同)</li><li>检查相应缓存, 有则取缓存, 无则远程下载(不是判断项目内node_modules缓存, 而是电脑全局的 <code>store</code> 通过 <code>npm config get cache</code>查看) 下载以及取缓存的都是压缩包,还有一个解压步骤</li><li>生成 <code>lockfiles</code></li></ol><p>🤔 有些依赖按 lock 安装 有些依赖按 <code>package.json</code> 安装吗？ 还是要么按 <code>package.json</code> 要么按 <code>lockfiles</code></p><h2 id="所有依赖安装到depen-不区分dev会有什么问题" tabindex="-1">所有依赖安装到depen....不区分dev会有什么问题？ <a class="header-anchor" href="#所有依赖安装到depen-不区分dev会有什么问题" aria-hidden="true">#</a></h2><ul><li><code>dependencies</code> 项目依赖</li><li><code>devDependencies</code> 开发依赖</li><li><code>peerDependencies</code> 同版本的建议依赖</li><li><code>bundledDependencies</code> 捆绑依赖</li><li><code>optionalDependencies</code> 可选依赖</li></ul><p><code>dependencies</code> 表示项目依赖，这些依赖都会成为你的线上生产环境中的代码组成的部分。当 它关联到 <code>npm</code> 包被下载的时候, <code>dependencies</code> 下的模块也会作为依赖, 一起被下载。</p><p>实际上, 依赖是否是被打包,完全是取决你的项目里的是否是被引入了该模块</p><p>而 <code>devDependencies</code> 中的依赖一定不会被打包的</p><p>👇 得看项目</p><ul><li>如果是前端 <code>spa</code> 应用 或者一次性的 <code>ssg</code> 项目可以这样做</li><li>但是如果是发布为依赖库就需要特别注意到底依赖是 <code>devDependencies</code> 还是仅生产环境下的依赖 <code>dependencies</code></li></ul><h2 id="同时用npm和yarn会有什么问题" tabindex="-1">同时用npm和yarn会有什么问题？ <a class="header-anchor" href="#同时用npm和yarn会有什么问题" aria-hidden="true">#</a></h2><p>antfu 的 vitesse 需要通过包的锁文件去判断具体用到那个包管理器然后用这个包管理器去自动安装具体的图标集依赖</p><p>不同的包管理器的 网络机制 缓存机制 下载后的依赖分布 不同，如果特别依赖这些的项目也需要注意一下</p><h2 id="是否应该提交lockfiles到仓库-删除node-modules和lockfiles-重新install-是否有风险" tabindex="-1">是否应该提交lockfiles到仓库？ 删除node_modules和lockfiles，重新install，是否有风险？ <a class="header-anchor" href="#是否应该提交lockfiles到仓库-删除node-modules和lockfiles-重新install-是否有风险" aria-hidden="true">#</a></h2><p>首先确定 <code>lockfiles</code> 的作用</p><ul><li><code>lockfiles</code> 用于保持依赖版本</li><li><code>lockfiles</code> 是依赖分析后的文件，有 <code>lockfiles</code> 就不用再做一次依赖分析，构建依赖树清单(扁平化), 提升一点速度</li></ul><p>比对 <code>lockfiles</code> 和 <code>package.json</code> 的版本，一般都是落后于 <code>package.json</code> 才不一致(手动升级了某个依赖)</p><p>此时会判断 <code>lockfiles</code> 落后的版本在 <code>package.json</code> 那里是否兼容，兼容的话不按package.json的新版本依赖来安装, 依然取 <code>lockfiles</code> (比对之后会相应的更新lock中的版本)</p><p>👆 注意流程图中不同 npm 版本处理 lock 比对版本的处理不相同，因此不同电脑的 npm 应该尽量相同，避免 lock 比对过程，每个人安装的依赖版本不同(有些按照 lock 有些按照 package)</p><p>假设没有 <code>lockfiles</code>, <code>package.json</code> 中的依赖清单版本需要限制具体版本, 才能避免安装出不同依赖的问题</p><p>但是嵌套依赖则没办法限制, 还是会出现过一段时间后, 重新安装的依赖跟以前不同(嵌套依赖升级了)</p><p>因此为了保证项目的长久稳定, 应该提交 lock 到仓库, 当需要升级某个依赖时, 更新 lock 及 <code>package.json</code> 提交</p><p>如果他人因为 npm 版本不同, 按照 lock 安装不了依赖</p><p>或按照 lock 成功安装了依赖, 但是运行项目时依赖报错, 此时可能是该依赖版本不兼容这个 nodejs 环境, 此时可以整个项目考虑兼容这个 nodejs 环境去对这个依赖升级或是降级,并提交调整后的 <code>package.json</code> 和 lock, 考虑不兼容这个 nodejs 环境的话, 就让对方换成可以运行的 nodejs 版本</p><h2 id="npm-yarn-扁平化方案" tabindex="-1">npm/yarn 扁平化方案 <a class="header-anchor" href="#npm-yarn-扁平化方案" aria-hidden="true">#</a></h2><p>其实很早期 <code>npm</code> 和 <code>yarn</code> 就采用扁平 <code>node_modules</code> 结构而不是 嵌套 <code>node_modules</code> 结构了</p><p>而这解决了一些问题也带来了一些问题</p><h3 id="解决了原问题" tabindex="-1">解决了原问题 <a class="header-anchor" href="#解决了原问题" aria-hidden="true">#</a></h3><ul><li>重复安装</li><li>依赖层级太深, 路径过长, windows系统会出现一些问题</li></ul><h3 id="带来了新问题" tabindex="-1">带来了新问题 <a class="header-anchor" href="#带来了新问题" aria-hidden="true">#</a></h3><ul><li><code>幽灵依赖</code>问题: 项目的 <code>package.json</code> 中没有引用的依赖，出现在 <code>node_modules</code> 目录下, 此时项目可以直接使用该依赖(也就是 <code>package.json</code> 不能描述准确依赖清单了, 这直接违反了 <code>package.json</code> 包管理的设计初衷) <ul><li>当某个依赖升级后不再依赖那个幽灵依赖时, 项目内如果使用了, 将报错</li><li>不了解幽灵依赖的具体版本, 如幽灵依赖版本较低, 项目内按照最新版本文档使用, 可能无法使用</li></ul></li><li><code>分身依赖</code>问题: 扁平依赖限制不同名, 也就是同名不同版本的依赖不会扁平到 <code>node_modules</code> , 而是仍然嵌套到具体的依赖报目录里 <ul><li>重复安装</li><li>重复的 <code>Typescript</code> 类型可能不同会互相冲突</li></ul></li></ul><p>👆 可以看出并 <strong>不是彻底的扁平化</strong></p><blockquote><p>🤔 应用依赖A、B，A也依赖B，会不会重复打包B？</p><p>取决于 <code>分身依赖</code> 的同名是否同版本的判断, 若同版本不会重复安装, 不同版本就会</p></blockquote><h2 id="pnpm" tabindex="-1">pnpm <a class="header-anchor" href="#pnpm" aria-hidden="true">#</a></h2><p><code>npm/yarn</code> 虽然都能创建 <code>软/硬链接</code>, 但是这个功能是用于用户自己设置要软链接的依赖</p><p>而 <code>pnpm</code> 则内部利用 <code>软/硬链接</code> 设置了完善的依赖存储方案</p><h3 id="概念-inode" tabindex="-1">概念 inode <a class="header-anchor" href="#概念-inode" aria-hidden="true">#</a></h3><p><code>inode</code> 每个文件都有一个唯一的 <code>inode</code>, 包含文件元信息, 当访问文件时, 对应的元信息会被 copy 到内存去实现访问</p><p><code>stat xxx.md</code> 可以查看具体文件的元信息</p><h3 id="概念-hard-soft-link" tabindex="-1">概念 hard/soft link <a class="header-anchor" href="#概念-hard-soft-link" aria-hidden="true">#</a></h3><p><code>Linux</code> 中包括两种链接：</p><ol><li>硬链接(<code>hard link</code>) <ul><li>创建多个空文件, 这些文件的 <code>inode</code> 等同于源文件 <code>inode</code>, 意味着只要源文件修改这些空文件也会同步修改</li><li>并且这个指向是双向的, 只要链接数非 0, 文件就一直存在</li></ul></li><li>软链接(<code>soft link</code>)，软链接又称为符号链接（symbolic link） <ul><li>创建多个空文件, 这些文件的 <code>inode</code> 是新的唯一的 <code>inode</code>, 永远指向(<code>inode</code>相同的指向可以理解, 新的<code>inode</code>怎么指向)源文件</li><li>这个指向是单向的, 删除 <code>soft link</code> 不影响源文件</li></ul></li></ol><h3 id="pnpm-install-安装过程" tabindex="-1">pnpm install 安装过程 <a class="header-anchor" href="#pnpm-install-安装过程" aria-hidden="true">#</a></h3><p><code>pnpm install</code> 安装时会判断全局的 <code>store</code> 中是否已存在对应的依赖</p><ul><li>存在则在 <code>node_modules/.pnpm</code> 创建一个 <code>hard link</code></li><li>不存在则下载 并安装到 <code>store</code> 中, 项目中的 <code>node_modules/.pnpm</code> 依然创建的是一个 <code>hard link</code></li></ul><p>按照👆的说法, <code>node_modules/.pnpm</code> 内部应该全都是 <code>hard link</code></p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki material-palenight"><code><span class="line"><span style="color:#A6ACCD;">node_modules</span></span>
<span class="line"><span style="color:#A6ACCD;">├─ .pnpm</span></span>
<span class="line"><span style="color:#A6ACCD;">│   └─ dayjs@1.10.7</span></span>
<span class="line"><span style="color:#A6ACCD;">└─ dayjs</span></span>
<span class="line"><span style="color:#A6ACCD;"></span></span></code></pre></div><h3 id="解决-幽灵依赖-问题" tabindex="-1">解决 幽灵依赖 问题 <a class="header-anchor" href="#解决-幽灵依赖-问题" aria-hidden="true">#</a></h3><p>而 <code>node_modules</code> 外层 非 <code>.pnpm/</code> 目录下的依赖包, 是 <code>package.json</code> 中依赖清单中对应的包</p><p>而 上面提到的 <code>幽灵依赖</code> 问题, 非 <code>package.json</code> 中的依赖在 <code>node_modules/.pnpm</code> 中才有, 外层没有</p><p>因此按照 nodejs 对 <code>bare module</code> 向上寻址是找不到依赖的, 项目内使用就会报错, 也就解决了 <code>幽灵依赖</code> 的问题</p><p>并且外层的 <code>package.json</code> 依赖包, 是 <code>soft link</code> 到 <code>node_modules/.pnpm</code>的对应依赖</p><p>🤔 为什么不是 <code>hard link</code></p><h3 id="解决分身依赖问题" tabindex="-1">解决分身依赖问题 <a class="header-anchor" href="#解决分身依赖问题" aria-hidden="true">#</a></h3><p>上面提到 <code>npm/yarn</code> 的依赖是不彻底的扁平化, 当依赖同名不同版本时, 会安装到各自依赖内部</p><p><code>pnpm</code> 安装的 <code>node_modules/.pnpm</code> 是嵌套结构, 连不彻底的扁平都不考虑</p><p><code>pnpm</code> 在外层 名称 不带版本</p><p>但是 <code>store</code> 以及 <code>node_modules/.pnpm</code> 中的依赖包名是带版本的</p><p>也就是相同的判断方法, 依赖同名(包括版本), 会安装到各自依赖内部, 这会是一个 <code>soft link</code> 直接取 <code>node_modules/.pnpm</code> 中的依赖( <code>hard link</code> 到 <code>store</code> )</p><p>这就解决了重复安装的问题</p><h3 id="更快的原因" tabindex="-1">更快的原因 <a class="header-anchor" href="#更快的原因" aria-hidden="true">#</a></h3><ul><li><code>npm/yarn</code> 也在本地有缓存, 但是 <code>pnpm</code> 依然比他们快。是因为 <code>npm/yarn</code> 取到缓存,还需要解压并复制文件到项目内，而 <code>pnpm</code> 只用创建 <code>hard link</code></li><li>首次安装依赖 <code>pnpm</code> 也比 <code>npm/yarn</code> 快 则是因为 <code>pnpm</code> 可能有缓存嵌套依赖, 而 <code>npm/yarn</code> 缓存嵌套依赖的机制没有这么全面</li></ul><h3 id="包存储在了-store-中-为什么我的-node-modules-还是占用了磁盘空间" tabindex="-1">包存储在了 <code>store</code> 中，为什么我的 <code>node_modules</code> 还是占用了磁盘空间？ <a class="header-anchor" href="#包存储在了-store-中-为什么我的-node-modules-还是占用了磁盘空间" aria-hidden="true">#</a></h3><p><code>pnpm</code> 创建从 <code>store</code> 到项目下 <code>node_modules</code> 文件夹的硬链接，但是硬链接本质还是和原始文件共享的是相同的 <code>inode</code></p><p>因此，它们二者其实是共享同一个空间的，看起来占用了 <code>node_modules</code> 的空间</p><p>所有始终只会占用一份空间，而不是两份</p><p><img src="https://kingan-md-img.oss-cn-guangzhou.aliyuncs.com/blog/20230113103145.png" alt=""></p><h2 id="参考资料" tabindex="-1">参考资料 <a class="header-anchor" href="#参考资料" aria-hidden="true">#</a></h2><ul><li><a href="https://juejin.cn/post/7060844948316225572" target="_blank" rel="noreferrer">字节的一个小问题 npm 和 yarn不一样吗？</a></li><li><a href="https://juejin.cn/post/7036319707590295565" target="_blank" rel="noreferrer">pnpm 解决我哪些痛点？</a></li></ul>`,70),i=[n];function a(p,s,r,t,h,m){return o(),d("div",null,i)}const f=e(c,[["render",a]]);export{k as __pageData,f as default};
